package com.electra.template.android.features.todo.detail

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

// TEMPORARY STUB — Task 3.4 implements real TodoDetailScreen
@Composable
fun TodoDetailScreen(id: String?, onDismiss: () -> Unit) {
    Text("TodoDetailScreen stub — id=$id — Task 3.4")
}
