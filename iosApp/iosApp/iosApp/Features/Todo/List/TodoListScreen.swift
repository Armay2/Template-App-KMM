import SwiftUI

// TEMPORARY STUB — Task 4.3 replaces this with the real container.
struct TodoListScreen: View {
    var body: some View {
        Text("TodoListScreen stub — Task 4.3")
            .navigationTitle("Todos")
    }
}
