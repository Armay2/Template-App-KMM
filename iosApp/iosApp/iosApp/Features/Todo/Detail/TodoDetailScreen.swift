import SwiftUI

// TEMPORARY STUB — Task 4.3 replaces this with the real container.
struct TodoDetailScreen: View {
    let id: String?
    var body: some View {
        Text("TodoDetailScreen stub — id=\(id ?? "new") — Task 4.3")
    }
}
